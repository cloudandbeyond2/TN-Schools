  const renderNeonTheme = () => (
    <div className="bg-slate-50 dark:bg-[#0b0c10] text-slate-800 dark:text-slate-200 rounded-3xl shadow-xl overflow-hidden border border-slate-200 dark:border-slate-800 p-8 md:p-12 relative font-sans transition-colors duration-500">
      {/* Background Glows (visible mainly in dark mode) */}
      <div className="absolute top-0 left-1/4 w-96 h-96 bg-cyan-200/50 dark:bg-cyan-500/10 rounded-full blur-[120px] pointer-events-none"></div>
      <div className="absolute bottom-0 right-1/4 w-96 h-96 bg-indigo-200/50 dark:bg-indigo-500/10 rounded-full blur-[120px] pointer-events-none"></div>

      {/* Header Section */}
      <div className="relative z-10 mb-12 border-b border-slate-200 dark:border-white/10 pb-8">
        <h1 className="text-5xl md:text-7xl font-black text-transparent bg-clip-text bg-gradient-to-r from-cyan-600 to-blue-600 dark:from-cyan-400 dark:to-blue-500 tracking-tight uppercase mb-2">
          {infographicData.title}
        </h1>
        <h2 className="text-xl md:text-2xl text-cyan-600 dark:text-cyan-300 font-bold mb-6">{infographicData.subtitle}</h2>
        <div className="flex flex-col md:flex-row gap-8 items-start">
          <p className="text-lg text-slate-600 dark:text-slate-300 leading-relaxed flex-1 max-w-2xl font-medium">
            {infographicData.introduction}
          </p>
          <div className="w-full md:w-1/3 flex justify-center">
             {infographicData.centralImageUrl && (
                <div className="relative w-48 h-48 rounded-full overflow-hidden shadow-xl ring-4 ring-cyan-100 dark:ring-cyan-500/30">
                  <img 
                    src={`https://image.pollinations.ai/prompt/${encodeURIComponent(infographicData.centralImageUrl)}?width=400&height=400&nologo=true`} 
                    alt={infographicData.title}
                    className="w-full h-full object-cover"
                    onError={(e) => { (e.currentTarget.parentElement as HTMLElement).style.display = 'none'; }}
                  />
                </div>
             )}
          </div>
        </div>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-12 gap-8 relative z-10 mb-12">
        {/* Left Column: What is it */}
        <div className="col-span-1 md:col-span-4 space-y-6">
          <h3 className="text-sm font-bold tracking-widest text-emerald-600 dark:text-emerald-400 uppercase mb-4 flex items-center gap-2">
            <i className="fi fi-rr-interrogation"></i>
            What is {infographicData.title}?
          </h3>
          {infographicData.whatIsIt?.map((item: any, i: number) => (
            <div key={i} className="flex items-start gap-4">
               <div className="w-10 h-10 rounded-full bg-emerald-100 dark:bg-emerald-500/10 border border-emerald-200 dark:border-emerald-500/20 flex items-center justify-center shrink-0 shadow-sm">
                 <i className={`fi ${item.icon} text-emerald-600 dark:text-emerald-400 text-lg`}></i>
               </div>
               <p className="text-sm text-slate-700 dark:text-slate-300 leading-relaxed pt-1 font-medium">{item.text}</p>
            </div>
          ))}
        </div>

        {/* Middle/Right Column: Law / Formula */}
        <div className="col-span-1 md:col-span-8">
          <div className="bg-gradient-to-br from-slate-100 to-white dark:from-slate-900/80 dark:to-slate-800/50 backdrop-blur-md border border-cyan-200 dark:border-cyan-500/20 rounded-2xl p-8 h-full flex flex-col justify-center shadow-lg">
            <h3 className="text-cyan-700 dark:text-cyan-400 font-bold uppercase tracking-wider mb-1">{infographicData.lawOrFormula?.title}</h3>
            <p className="text-slate-500 dark:text-slate-400 text-sm mb-6 font-medium">{infographicData.lawOrFormula?.subtitle}</p>
            
            <div className="bg-slate-50 dark:bg-black/40 border border-slate-200 dark:border-white/5 rounded-xl p-6 flex flex-col items-center justify-center mb-6 shadow-inner">
              <span className="text-4xl md:text-5xl font-serif text-slate-800 dark:text-white font-bold">{infographicData.lawOrFormula?.formula}</span>
            </div>

            <div className="grid grid-cols-1 sm:grid-cols-2 gap-x-8 gap-y-3">
              {infographicData.lawOrFormula?.variables?.map((v: any, i: number) => (
                <div key={i} className="flex items-baseline gap-3">
                  <span className="text-cyan-600 dark:text-cyan-300 font-bold font-serif w-6">{v.symbol}</span>
                  <span className="text-slate-600 dark:text-slate-400 text-sm font-medium">= {v.explanation}</span>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>

      {/* Key Facts and Did you know */}
      <div className="grid grid-cols-1 md:grid-cols-12 gap-8 relative z-10 mb-12">
         <div className="col-span-1 md:col-span-8">
            <div className="flex items-center gap-4 mb-6">
              <div className="h-px bg-cyan-200 dark:bg-cyan-500/30 flex-1"></div>
              <h3 className="text-sm font-bold tracking-widest text-cyan-600 dark:text-cyan-400 uppercase">Key Facts</h3>
              <div className="h-px bg-cyan-200 dark:bg-cyan-500/30 flex-1"></div>
            </div>
            <div className="grid grid-cols-2 md:grid-cols-4 gap-6">
               {infographicData.keyFacts?.slice(0,4).map((fact: any, i: number) => (
                  <div key={i} className="text-center flex flex-col items-center group">
                     <div className="w-16 h-16 rounded-2xl bg-white dark:bg-slate-800/50 border border-slate-200 dark:border-slate-700 flex items-center justify-center mb-4 group-hover:bg-cyan-50 dark:group-hover:bg-cyan-900/30 group-hover:border-cyan-300 dark:group-hover:border-cyan-500/50 transition-all duration-300 shadow-sm">
                       <i className={`fi ${fact.icon} text-2xl text-cyan-500 dark:text-cyan-400`}></i>
                     </div>
                     <h4 className="text-slate-800 dark:text-white font-bold text-sm mb-2">{fact.title}</h4>
                     <p className="text-xs text-slate-600 dark:text-slate-400 leading-relaxed">{fact.description}</p>
                  </div>
               ))}
            </div>
         </div>

         <div className="col-span-1 md:col-span-4">
            <div className="bg-gradient-to-br from-pink-50 to-purple-50 dark:from-pink-900/20 dark:to-purple-900/20 border border-pink-200 dark:border-pink-500/20 rounded-2xl p-6 h-full shadow-lg flex flex-col">
               <div className="flex items-center gap-3 mb-4">
                 <i className="fi fi-rr-lightbulb text-pink-500 dark:text-pink-400 text-2xl animate-pulse"></i>
                 <h3 className="text-pink-600 dark:text-pink-400 font-bold uppercase tracking-wider text-sm">Did You Know?</h3>
               </div>
               <p className="text-slate-700 dark:text-slate-300 text-sm leading-relaxed font-medium">{infographicData.didYouKnow?.description}</p>
            </div>
         </div>
      </div>

      {/* Examples in Action */}
      <div className="relative z-10 mb-12">
         <h3 className="text-sm font-bold tracking-widest text-emerald-600 dark:text-emerald-400 uppercase mb-6">Examples in Action</h3>
         <div className="grid grid-cols-2 md:grid-cols-5 gap-4">
           {infographicData.examples?.map((ex: any, i: number) => (
             <div key={i} className="bg-white dark:bg-slate-900/50 rounded-xl overflow-hidden border border-slate-200 dark:border-slate-800 group hover:border-emerald-300 dark:hover:border-emerald-500/50 transition-all shadow-sm">
               <div className="h-32 bg-slate-200 dark:bg-slate-800 relative overflow-hidden">
                  <img 
                    src={`https://image.pollinations.ai/prompt/${encodeURIComponent(ex.imageKeyword)}?width=400&height=300&nologo=true`} 
                    alt={ex.title}
                    className="w-full h-full object-cover opacity-90 dark:opacity-70 group-hover:opacity-100 group-hover:scale-110 transition-all duration-500"
                    onError={(e) => { (e.currentTarget.parentElement as HTMLElement).style.display = 'none'; }}
                  />
               </div>
               <div className="p-4">
                  <h4 className="text-slate-800 dark:text-white font-bold text-sm mb-1">{ex.title}</h4>
                  <p className="text-xs text-slate-500 dark:text-slate-400">{ex.description}</p>
               </div>
             </div>
           ))}
         </div>
      </div>

      {/* Applications & Quote */}
      <div className="grid grid-cols-1 md:grid-cols-12 gap-8 relative z-10 border-t border-slate-200 dark:border-white/10 pt-8">
         <div className="col-span-1 md:col-span-5">
           <h3 className="text-sm font-bold tracking-widest text-cyan-600 dark:text-cyan-400 uppercase mb-6">Applications</h3>
           <div className="space-y-4">
             {infographicData.applications?.map((app: any, i: number) => (
               <div key={i} className="flex items-center gap-4">
                  <div className="w-10 h-10 rounded-full bg-orange-100 dark:bg-orange-500/10 border border-orange-200 dark:border-orange-500/20 flex items-center justify-center shrink-0 shadow-sm">
                    <i className={`fi ${app.icon} text-orange-500 dark:text-orange-400`}></i>
                  </div>
                  <div>
                    <h4 className="text-sm font-bold text-slate-800 dark:text-white">{app.title}</h4>
                    <p className="text-xs text-slate-500 dark:text-slate-400">{app.description}</p>
                  </div>
               </div>
             ))}
           </div>
         </div>

         <div className="col-span-1 md:col-span-7 flex flex-col justify-between">
            <div className="pl-6 border-l-4 border-indigo-300 dark:border-indigo-500/50 mb-8">
               <p className="text-xl md:text-2xl text-slate-700 dark:text-slate-300 font-serif italic mb-4 leading-relaxed font-medium">
                 "{infographicData.quote?.text}"
               </p>
               <p className="text-indigo-600 dark:text-indigo-400 font-bold">— {infographicData.quote?.author}</p>
            </div>
            <div className="bg-indigo-50 dark:bg-indigo-900/20 border border-indigo-100 dark:border-indigo-500/20 rounded-xl p-6 shadow-sm">
               <h4 className="text-indigo-700 dark:text-indigo-400 font-bold uppercase tracking-wider text-xs mb-2">{infographicData.remember?.title}</h4>
               <p className="text-sm text-slate-700 dark:text-slate-300 leading-relaxed font-medium">{infographicData.remember?.text}</p>
            </div>
         </div>
      </div>
    </div>
  );

  const renderAcademicTheme = () => (
    <div className="bg-white dark:bg-slate-900 text-slate-900 dark:text-slate-100 rounded-lg shadow-sm border border-slate-200 dark:border-slate-800 p-10 md:p-16 max-w-4xl mx-auto font-serif">
      <div className="text-center mb-12 border-b-2 border-slate-800 dark:border-slate-200 pb-8">
        <h1 className="text-4xl md:text-6xl font-bold uppercase tracking-wide mb-4">{infographicData.title}</h1>
        <h2 className="text-xl md:text-2xl italic text-slate-600 dark:text-slate-400">{infographicData.subtitle}</h2>
      </div>

      <div className="mb-12">
        <p className="text-lg leading-relaxed first-letter:text-5xl first-letter:font-bold first-letter:mr-1 first-letter:float-left">
          {infographicData.introduction}
        </p>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-2 gap-12 mb-12">
        <div>
          <h3 className="text-2xl font-bold border-b border-slate-300 dark:border-slate-700 pb-2 mb-6">I. Definitions</h3>
          <ul className="list-disc pl-5 space-y-4">
            {infographicData.whatIsIt?.map((item: any, i: number) => (
              <li key={i} className="text-slate-700 dark:text-slate-300 leading-relaxed">{item.text}</li>
            ))}
          </ul>
        </div>
        <div>
           <h3 className="text-2xl font-bold border-b border-slate-300 dark:border-slate-700 pb-2 mb-6">II. {infographicData.lawOrFormula?.title}</h3>
           <div className="bg-slate-50 dark:bg-slate-800 p-6 rounded-md mb-6 border border-slate-200 dark:border-slate-700 text-center">
             <span className="text-3xl font-bold">{infographicData.lawOrFormula?.formula}</span>
           </div>
           <p className="text-sm text-slate-600 dark:text-slate-400 mb-4 italic">{infographicData.lawOrFormula?.subtitle}</p>
           <ul className="space-y-2 text-sm">
             {infographicData.lawOrFormula?.variables?.map((v: any, i: number) => (
               <li key={i}><span className="font-bold mr-2">{v.symbol}:</span> {v.explanation}</li>
             ))}
           </ul>
        </div>
      </div>

      <div className="mb-12">
         <h3 className="text-2xl font-bold border-b border-slate-300 dark:border-slate-700 pb-2 mb-6 text-center">III. Empirical Facts</h3>
         <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
            {infographicData.keyFacts?.slice(0,3).map((fact: any, i: number) => (
               <div key={i} className="border border-slate-200 dark:border-slate-700 p-4 rounded bg-slate-50 dark:bg-slate-800/50">
                  <h4 className="font-bold mb-2">{fact.title}</h4>
                  <p className="text-sm text-slate-700 dark:text-slate-300">{fact.description}</p>
               </div>
            ))}
         </div>
      </div>

      <div className="mb-12 flex flex-col md:flex-row gap-8 items-center bg-slate-100 dark:bg-slate-800 p-8 rounded-lg">
        <div className="flex-1">
          <h3 className="text-2xl font-bold mb-4">{infographicData.didYouKnow?.title}</h3>
          <p className="text-slate-700 dark:text-slate-300 leading-relaxed">{infographicData.didYouKnow?.description}</p>
        </div>
        <div className="w-48 h-48 rounded-full overflow-hidden shrink-0 shadow-md">
           <img 
             src={`https://image.pollinations.ai/prompt/${encodeURIComponent(infographicData.centralImageUrl)}?width=400&height=400&nologo=true`} 
             alt="Figure 1" 
             className="w-full h-full object-cover grayscale opacity-80" 
             onError={(e) => { (e.currentTarget.parentElement as HTMLElement).style.display = 'none'; }}
           />
        </div>
      </div>

      <div className="border-t-2 border-slate-800 dark:border-slate-200 pt-8 text-center">
        <p className="text-2xl italic mb-4">"{infographicData.quote?.text}"</p>
        <p className="font-bold">— {infographicData.quote?.author}</p>
      </div>
    </div>
  );

  const renderBentoTheme = () => (
    <div className="bg-slate-100 dark:bg-[#111] p-4 md:p-8 rounded-3xl font-sans">
      <div className="grid grid-cols-1 md:grid-cols-4 md:grid-rows-4 gap-4 max-w-6xl mx-auto auto-rows-fr">
        
        {/* Title Card */}
        <div className="col-span-1 md:col-span-2 md:row-span-1 bg-indigo-600 dark:bg-indigo-600 text-white p-8 rounded-3xl flex flex-col justify-center shadow-md">
          <h1 className="text-4xl md:text-5xl font-black mb-2">{infographicData.title}</h1>
          <p className="text-indigo-100 font-medium">{infographicData.subtitle}</p>
        </div>

        {/* Intro Card */}
        <div className="col-span-1 md:col-span-2 md:row-span-1 bg-white dark:bg-slate-900 text-slate-800 dark:text-slate-200 p-8 rounded-3xl shadow-sm border border-slate-200 dark:border-slate-800 flex items-center">
          <p className="text-sm md:text-base font-medium leading-relaxed">{infographicData.introduction}</p>
        </div>

        {/* Image Card */}
        <div className="col-span-1 md:col-span-1 md:row-span-2 bg-slate-200 dark:bg-slate-800 rounded-3xl overflow-hidden shadow-sm relative group">
           <img 
             src={`https://image.pollinations.ai/prompt/${encodeURIComponent(infographicData.centralImageUrl)}?width=600&height=800&nologo=true`} 
             className="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110" 
             alt="Main" 
             onError={(e) => { (e.currentTarget.parentElement as HTMLElement).style.display = 'none'; }}
           />
           <div className="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent"></div>
           <h3 className="absolute bottom-6 left-6 text-white font-bold text-xl">{infographicData.title}</h3>
        </div>

        {/* Law Card */}
        <div className="col-span-1 md:col-span-2 md:row-span-1 bg-emerald-500 dark:bg-emerald-600 text-white p-8 rounded-3xl shadow-sm flex flex-col justify-center items-center text-center">
          <h3 className="text-sm font-bold uppercase tracking-wider mb-2 opacity-80">{infographicData.lawOrFormula?.title}</h3>
          <span className="text-4xl font-serif font-bold">{infographicData.lawOrFormula?.formula}</span>
        </div>

        {/* Did You Know Card */}
        <div className="col-span-1 md:col-span-1 md:row-span-1 bg-amber-400 dark:bg-amber-500 text-amber-950 p-6 rounded-3xl shadow-sm flex flex-col justify-center">
          <h3 className="font-bold uppercase tracking-wider mb-2 flex items-center gap-2">
            <i className="fi fi-rr-lightbulb"></i> Fact
          </h3>
          <p className="text-sm font-medium leading-relaxed">{infographicData.didYouKnow?.description}</p>
        </div>

        {/* Key Facts list */}
        <div className="col-span-1 md:col-span-1 md:row-span-2 bg-white dark:bg-slate-900 text-slate-800 dark:text-slate-200 p-6 rounded-3xl shadow-sm border border-slate-200 dark:border-slate-800 overflow-hidden">
          <h3 className="font-bold mb-4 uppercase tracking-wider text-sm text-slate-500">Key Points</h3>
          <ul className="space-y-4">
            {infographicData.whatIsIt?.map((item: any, i: number) => (
              <li key={i} className="flex gap-3 text-sm font-medium">
                <i className={`fi ${item.icon} text-indigo-500 shrink-0 pt-1`}></i>
                <span className="leading-relaxed">{item.text}</span>
              </li>
            ))}
          </ul>
        </div>

        {/* Quote Card */}
        <div className="col-span-1 md:col-span-2 md:row-span-1 bg-slate-900 dark:bg-black text-white p-8 rounded-3xl shadow-sm flex flex-col justify-center items-center text-center border border-slate-800">
           <p className="text-lg md:text-xl font-serif italic mb-4">"{infographicData.quote?.text}"</p>
           <p className="text-sm font-bold text-slate-400">— {infographicData.quote?.author}</p>
        </div>

      </div>
    </div>
  );

  const renderTimelineTheme = () => (
    <div className="bg-slate-50 dark:bg-slate-900 text-slate-800 dark:text-slate-200 rounded-3xl shadow-md border border-slate-200 dark:border-slate-800 p-8 md:p-12 max-w-3xl mx-auto font-sans">
      <div className="text-center mb-16">
        <h1 className="text-5xl font-black text-slate-900 dark:text-white mb-4">{infographicData.title}</h1>
        <p className="text-xl text-emerald-600 dark:text-emerald-400 font-medium mb-6">{infographicData.subtitle}</p>
        <p className="text-lg leading-relaxed text-slate-600 dark:text-slate-400 max-w-2xl mx-auto">{infographicData.introduction}</p>
      </div>

      <div className="relative border-l-4 border-emerald-500 dark:border-emerald-600 ml-4 md:ml-12 space-y-16 py-8">
        
        {/* Step 1 */}
        <div className="relative pl-8 md:pl-16">
          <div className="absolute -left-3.5 top-0 w-6 h-6 rounded-full bg-emerald-500 border-4 border-white dark:border-slate-900 shadow"></div>
          <h3 className="text-2xl font-bold mb-6 text-slate-900 dark:text-white">Core Concepts</h3>
          <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
            {infographicData.whatIsIt?.map((item: any, i: number) => (
              <div key={i} className="bg-white dark:bg-slate-800 p-5 rounded-2xl shadow-sm border border-slate-100 dark:border-slate-700">
                <i className={`fi ${item.icon} text-emerald-500 text-2xl mb-3 block`}></i>
                <p className="text-sm font-medium">{item.text}</p>
              </div>
            ))}
          </div>
        </div>

        {/* Step 2 */}
        <div className="relative pl-8 md:pl-16">
          <div className="absolute -left-3.5 top-0 w-6 h-6 rounded-full bg-blue-500 border-4 border-white dark:border-slate-900 shadow"></div>
          <h3 className="text-2xl font-bold mb-6 text-slate-900 dark:text-white">The Governing Law</h3>
          <div className="bg-blue-50 dark:bg-blue-900/20 border border-blue-100 dark:border-blue-500/20 p-6 rounded-2xl shadow-sm">
             <h4 className="font-bold text-blue-700 dark:text-blue-400 mb-4">{infographicData.lawOrFormula?.title}</h4>
             <div className="text-3xl font-serif font-bold text-center mb-6">{infographicData.lawOrFormula?.formula}</div>
             <div className="space-y-2 text-sm text-slate-600 dark:text-slate-400 font-medium">
               {infographicData.lawOrFormula?.variables?.map((v: any, i: number) => (
                 <div key={i}><span className="font-bold text-slate-800 dark:text-white mr-2">{v.symbol} =</span>{v.explanation}</div>
               ))}
             </div>
          </div>
        </div>

        {/* Step 3 */}
        <div className="relative pl-8 md:pl-16">
          <div className="absolute -left-3.5 top-0 w-6 h-6 rounded-full bg-amber-500 border-4 border-white dark:border-slate-900 shadow"></div>
          <h3 className="text-2xl font-bold mb-6 text-slate-900 dark:text-white">Real World Examples</h3>
          <div className="space-y-4">
             {infographicData.examples?.slice(0,3).map((ex: any, i: number) => (
               <div key={i} className="flex gap-4 items-center bg-white dark:bg-slate-800 p-4 rounded-2xl shadow-sm border border-slate-100 dark:border-slate-700">
                  <img 
                    src={`https://image.pollinations.ai/prompt/${encodeURIComponent(ex.imageKeyword)}?width=100&height=100&nologo=true`} 
                    className="w-16 h-16 rounded-xl object-cover shrink-0" 
                    onError={(e) => { e.currentTarget.style.display = 'none'; }}
                  />
                  <div>
                    <h4 className="font-bold text-sm text-slate-900 dark:text-white">{ex.title}</h4>
                    <p className="text-xs text-slate-500 dark:text-slate-400">{ex.description}</p>
                  </div>
               </div>
             ))}
          </div>
        </div>

      </div>
    </div>
  );
